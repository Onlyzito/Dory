const { MessageEmbed } = require("discord.js");

module.exports = {
    name: "test",
    description: "Testar comandos.",
    permission: "SEND_MESSAGES",

    run: async (client, interaction) => {

    },
};
